[Unit]
Description=Story Ethereum go client
After=syslog.target network.target

[Service]
User={{ user }}
Type=simple
ExecStart=/usr/local/bin/geth --iliad --syncmode full
KillMode=process
KillSignal=SIGINT
TimeoutStopSec=90
Restart=on-failure
RestartSec=10s

[Install]
WantedBy=multi-user.target
